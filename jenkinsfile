pipeline {
    agent any

    environment {
        BENCH_DIR = "/home/deepika/frappe-bench"
        SITE_NAME = "new.local"
        BENCH_CMD = "/home/deepika/.local/bin/bench"
        PATH = "/usr/local/bin:${env.PATH}" // ensures yarn is found
    }

    stages {

        stage('Pull Latest Code') {
            steps {
                sh '''
                echo "🔄 Pulling latest changes from Git..."

                # Pull frappe app (develop branch)
                sudo -u deepika bash -c "cd ${BENCH_DIR}/apps/frappe && git fetch origin && git reset --hard origin/develop && git checkout -B develop origin/develop"

                # Pull erpnext app (version-14 branch)
                sudo -u deepika bash -c "cd ${BENCH_DIR}/apps/erpnext && git fetch origin && git reset --hard origin/version-14 && git checkout -B version-14 origin/version-14"
                '''
            }
        }

        stage('Clean Assets & Cache (Optional)') {
            steps {
                sh '''
                echo "🧹 Cleaning old assets and cache..."
                sudo -u deepika bash -c "cd ${BENCH_DIR} && ${BENCH_CMD} --site ${SITE_NAME} clear-cache && rm -rf sites/assets"
                '''
            }
        }

        stage('Build & Migrate') {
            steps {
                sh '''
                echo "⚙️ Building assets and migrating database..."
                sudo -u deepika bash -c "cd ${BENCH_DIR} && ${BENCH_CMD} build && ${BENCH_CMD} --site ${SITE_NAME} migrate"
                '''
            }
        }

        stage('Restart Bench') {
            steps {
                sh '''
                echo "🔁 Restarting Bench..."
                sudo -u deepika bash -c "cd ${BENCH_DIR} && ${BENCH_CMD} restart"
                '''
            }
        }
    }

    post {
        success {
            echo "✅ Deployment successful!"
        }
        failure {
            echo "❌ Deployment failed. Check logs."
        }
    }
}
